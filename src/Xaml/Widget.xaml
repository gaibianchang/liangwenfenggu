<?xml version="1.0" encoding="utf-8"?>
<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Width="380" Height="236">
  <Grid.Resources>
    <Style TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI, Microsoft YaHei"/>
    </Style>

    <Style x:Key="DarkMenu" TargetType="ContextMenu">
      <Setter Property="Background" Value="#F2101318"/>
      <Setter Property="Foreground" Value="#E8FFFFFF"/>
      <Setter Property="FontSize" Value="12.5"/>
      <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI"/>
      <Setter Property="BorderBrush" Value="#26FFFFFF"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="5"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ContextMenu">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="12"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="4" Direction="270" Opacity="0.55" Color="#000000"/>
              </Border.Effect>
              <StackPanel IsItemsHost="True"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="DarkMenuItem" TargetType="MenuItem">
      <Setter Property="Foreground" Value="#E8FFFFFF"/>
      <Setter Property="FontSize" Value="12.5"/>
      <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="MenuItem">
            <Border x:Name="Bd" CornerRadius="8" Background="Transparent" Padding="10,7" SnapsToDevicePixels="True">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="Chk" Grid.Column="0" Text="&#x2713;" FontFamily="Segoe UI Symbol" FontSize="11"
                           Width="16" Foreground="#FF4C8DFF" VerticalAlignment="Center" Visibility="Hidden"/>
                <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center"/>
              </Grid>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#1FFFFFFF"/>
              </Trigger>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Chk" Property="Visibility" Value="Visible"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="DarkSeparator" TargetType="Separator">
      <Setter Property="Height" Value="1"/>
      <Setter Property="Margin" Value="8,5"/>
      <Setter Property="Background" Value="#1FFFFFFF"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Separator">
            <Border Height="1" Background="{TemplateBinding Background}"/>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Grid.Resources>

  <Border x:Name="Card" Margin="14" CornerRadius="22" BorderThickness="1">
    <Border.Background>
      <LinearGradientBrush StartPoint="0,0" EndPoint="0.45,1">
        <GradientStop Offset="0" Color="#F21C2129"/>
        <GradientStop Offset="1" Color="#F20B0E13"/>
      </LinearGradientBrush>
    </Border.Background>
    <Border.BorderBrush>
      <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
        <GradientStop Offset="0" Color="#3AFFFFFF"/>
        <GradientStop Offset="1" Color="#10FFFFFF"/>
      </LinearGradientBrush>
    </Border.BorderBrush>
    <Border.Effect>
      <DropShadowEffect BlurRadius="30" ShadowDepth="4" Direction="270" Opacity="0.5" Color="#000000"/>
    </Border.Effect>

    <Grid>
      <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20,12">
        <TextBlock x:Name="DateText" FontSize="12.5" Foreground="#8AFFFFFF" HorizontalAlignment="Center"
                   Text="2026年9月17日 星期四"/>
        <TextBlock x:Name="TimeText" FontSize="32" FontWeight="SemiBold" Foreground="#FFFFFFFF"
                   HorizontalAlignment="Center" Margin="0,2,0,0" Typography.NumeralAlignment="Tabular"
                   Text="14:23:05"/>
        <TextBlock x:Name="NameText" FontSize="42" FontWeight="Bold" Foreground="#FFFFFFFF"
                   HorizontalAlignment="Center" Text="梁文峰"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,7,0,0">
          <TextBlock x:Name="CountdownLabel" FontSize="12" Foreground="#73FFFFFF" VerticalAlignment="Center"
                     Text="距梁文谷"/>
          <TextBlock x:Name="CountdownText" FontSize="12.5" FontWeight="SemiBold" Foreground="#E632D74B"
                     VerticalAlignment="Center" Margin="9,0,0,0" Typography.NumeralAlignment="Tabular" Text="01:23:45"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
          <Ellipse x:Name="StatusDot" Width="6" Height="6" Fill="#FF32D74B" VerticalAlignment="Center" Margin="0,1,7,0"/>
          <TextBlock x:Name="BalanceLabel" FontSize="13" Foreground="#8AFFFFFF" VerticalAlignment="Center"
                     Text="当前余额："/>
          <TextBlock x:Name="BalanceText" FontSize="13.5" FontWeight="SemiBold" Foreground="#F2FFFFFF"
                     VerticalAlignment="Center" Text="--"/>
        </StackPanel>
      </StackPanel>

      <TextBlock x:Name="GearButton" Text="&#x2699;" FontFamily="Segoe UI Symbol" FontSize="14"
                 Foreground="#D9FFFFFF" HorizontalAlignment="Right" VerticalAlignment="Top"
                 Margin="0,16,18,0" Opacity="0" Cursor="Hand"/>
    </Grid>
  </Border>
</Grid>
